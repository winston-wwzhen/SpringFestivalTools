<!--pages-gala/platforms/index.wxml-->
<view class="container">
  <nav-bar title="春晚节目单" bgColor="#D32F2F"></nav-bar>

  <!-- 装饰灯笼 -->
  <view class="lantern-left">🏮</view>
  <view class="lantern-right">🏮</view>

  <!-- 平台列表 -->
  <view class="platform-list">
    <block wx:if="{{list.length > 0}}">
      <view class="platform-card" wx:for="{{list}}" wx:key="id" bindtap="goToPrograms" data-id="{{item.id}}">
        <view class="card-cover">
          <image class="cover-image" src="{{item.cover_image || '/images/gala-default.png'}}" mode="aspectFill"></image>
          <view class="cover-badge" wx:if="{{item.is_live}}">
            <text class="live-dot"></text>
            <text>直播中</text>
          </view>
        </view>

        <view class="card-body">
          <view class="platform-header">
            <image class="platform-logo" src="{{item.logo || '/images/icon-platform.png'}}" mode="aspectFit"></image>
            <view class="platform-info">
              <view class="platform-name">{{item.name}}</view>
              <view class="platform-time">{{item.broadcast_time}}</view>
            </view>
          </view>

          <view class="program-count">
            <text class="count-num">{{item.program_count}}</text>
            <text class="count-text">个节目</text>
          </view>

          <view class="platform-tags" wx:if="{{item.tags && item.tags.length > 0}}">
            <view class="tag" wx:for="{{item.tags}}" wx:key="*this" wx:for-item="tag">{{tag}}</view>
          </view>
        </view>
      </view>
    </block>

    <!-- 空状态 -->
    <view class="empty-state" wx:if="{{list.length === 0 && !loading}}">
      <image class="empty-icon" src="/images/empty.png" mode="aspectFit"></image>
      <text class="empty-text">暂无春晚信息</text>
    </view>

    <!-- 加载中 -->
    <view class="loading-state" wx:if="{{loading}}">
      <text>加载中...</text>
    </view>
  </view>
</view>