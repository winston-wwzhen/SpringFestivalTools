<!--pages/toolbox/fortune/index.wxml-->
<view class="page">
  <!-- 自定义导航栏 -->
  <nav-bar title="新年运势" bgColor="#B71C1C" showBack="{{true}}"></nav-bar>

  <!-- 头部 -->
  <view class="header">
    <view class="header-bg"></view>
    <view class="header-content">
      <view class="title">🔮 新年运势</view>
      <view class="subtitle">抽取你的2026马年运势签</view>
    </view>
  </view>

  <!-- 抽签区域 -->
  <view class="draw-section" wx:if="{{!fortuneResult}}">
    <view class="fortune-box {{drawing ? 'drawing' : ''}}">
      <view class="fortune-lid {{drawing ? 'opening' : ''}}">🧧</view>
      <view class="fortune-body {{drawing ? 'drawing' : ''}}">
        <view class="fortune-sticks {{drawing ? 'shaking' : ''}}">
          <view class="stick" wx:for="{{6}}" wx:key="index"></view>
        </view>
      </view>
      <!-- 飞出的签纸效果 -->
      <view class="fortune-paper {{drawing ? 'show' : ''}}">📜</view>
    </view>

    <view class="draw-hint">
      <text class="hint-text">{{drawing ? '正在抽签中...' : '点击下方按钮抽取你的新年运势'}}</text>
    </view>

    <button class="btn-draw" bindtap="drawFortune" disabled="{{drawing}}">
      {{drawing ? '🔮 抽签中...' : '🎲 抽取新年签'}}
    </button>

    <view class="keyword-section">
      <view class="section-label">选择运势类型（可选）</view>
      <view class="keyword-group">
        <view class="keyword-item {{keyword === 'love' ? 'active' : ''}}" bindtap="onKeywordSelect" data-keyword="love">
          <text class="keyword-icon">❤️</text>
          <text class="keyword-text">爱情</text>
        </view>
        <view class="keyword-item {{keyword === 'career' ? 'active' : ''}}" bindtap="onKeywordSelect" data-keyword="career">
          <text class="keyword-icon">💼</text>
          <text class="keyword-text">事业</text>
        </view>
        <view class="keyword-item {{keyword === 'wealth' ? 'active' : ''}}" bindtap="onKeywordSelect" data-keyword="wealth">
          <text class="keyword-icon">💰</text>
          <text class="keyword-text">财富</text>
        </view>
        <view class="keyword-item {{keyword === 'health' ? 'active' : ''}}" bindtap="onKeywordSelect" data-keyword="health">
          <text class="keyword-icon">🏥</text>
          <text class="keyword-text">健康</text>
        </view>
        <view class="keyword-item {{keyword === 'study' ? 'active' : ''}}" bindtap="onKeywordSelect" data-keyword="study">
          <text class="keyword-icon">📚</text>
          <text class="keyword-text">学业</text>
        </view>
        <view class="keyword-item {{keyword === 'family' ? 'active' : ''}}" bindtap="onKeywordSelect" data-keyword="family">
          <text class="keyword-icon">👨‍👩‍👧‍👦</text>
          <text class="keyword-text">家庭</text>
        </view>
      </view>
    </view>
  </view>

  <!-- 运势结果 -->
  <view class="result-section" wx:if="{{fortuneResult && !drawing}}">
    <view class="result-header">
      <text class="result-title">🎊 2026马年运势签</text>
      <view class="fortune-rank {{fortuneResult.rankClass}}">{{fortuneResult.rank}}</view>
    </view>

    <!-- 综合运势 -->
    <view class="fortune-card overall">
      <view class="card-title">⭐ 综合运势</view>
      <view class="star-rating">
        <text wx:for="{{fortuneResult.overallStars}}" wx:key="index" class="star">★</text>
      </view>
      <view class="fortune-desc">{{fortuneResult.overfallDesc}}</view>
    </view>

    <!-- 各项运势 -->
    <view class="fortune-list">
      <view class="fortune-item">
        <view class="fortune-label">💰 财运</view>
        <view class="fortune-bar">
          <view class="bar-fill" style="width: {{fortuneResult.wealth}}%"></view>
        </view>
        <view class="fortune-value">{{fortuneResult.wealth}}%</view>
      </view>

      <view class="fortune-item">
        <view class="fortune-label">💼 事业</view>
        <view class="fortune-bar">
          <view class="bar-fill" style="width: {{fortuneResult.career}}%"></view>
        </view>
        <view class="fortune-value">{{fortuneResult.career}}%</view>
      </view>

      <view class="fortune-item">
        <view class="fortune-label">❤️ 爱情</view>
        <view class="fortune-bar">
          <view class="bar-fill" style="width: {{fortuneResult.love}}%"></view>
        </view>
        <view class="fortune-value">{{fortuneResult.love}}%</view>
      </view>

      <view class="fortune-item">
        <view class="fortune-label">🏥 健康</view>
        <view class="fortune-bar">
          <view class="bar-fill" style="width: {{fortuneResult.health}}%"></view>
        </view>
        <view class="fortune-value">{{fortuneResult.health}}%</view>
      </view>
    </view>

    <!-- 幸运元素 -->
    <view class="lucky-section">
      <view class="lucky-title">🍀 幸运元素</view>
      <view class="lucky-grid">
        <view class="lucky-item">
          <view class="lucky-label">幸运颜色</view>
          <view class="lucky-value">{{fortuneResult.luckyColor}}</view>
        </view>
        <view class="lucky-item">
          <view class="lucky-label">幸运数字</view>
          <view class="lucky-value">{{fortuneResult.luckyNumber}}</view>
        </view>
        <view class="lucky-item">
          <view class="lucky-label">幸运方位</view>
          <view class="lucky-value">{{fortuneResult.luckyDirection}}</view>
        </view>
        <view class="lucky-item">
          <view class="lucky-label">幸运生肖</view>
          <view class="lucky-value">{{fortuneResult.luckyZodiac}}</view>
        </view>
      </view>
    </view>

    <!-- 运势建议 -->
    <view class="advice-section">
      <view class="advice-title">💡 新年寄语</view>
      <view class="advice-content">{{fortuneResult.advice}}</view>
    </view>

    <!-- 操作按钮 -->
    <view class="action-buttons">
      <button class="btn-action" bindtap="drawFortune" disabled="{{loading}}">
        🔄 再抽一次
      </button>
    </view>
  </view>
</view>
