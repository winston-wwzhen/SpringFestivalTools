/* pages/index/index.wxss */

.page {
  min-height: 100vh;
  background: var(--bg-color);
  padding-bottom: 40rpx;
}

/* 头部区域 - 优化渐变与质感 */
/* 头部区域 - 优化渐变与质感 */
.header {
  position: relative;
  overflow: hidden;
  /* 增加顶部内边距以适配状态栏 (假设状态栏~44px + 胶囊~44px) */
  padding: 180rpx 40rpx 100rpx 40rpx; 
  background: linear-gradient(135deg, #B71C1C 0%, #D32F2F 50%, #FF5252 100%);
  border-bottom-left-radius: 48rpx;
  border-bottom-right-radius: 48rpx;
  box-shadow: 0 10rpx 40rpx rgba(211, 47, 47, 0.25);
  margin-bottom: 20rpx;
}

/* 背景装饰层 */
.header__bg {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  overflow: hidden;
}

.bg-circle {
  position: absolute;
  border-radius: 50%;
  pointer-events: none;
}

.bg-circle--1 {
  width: 500rpx;
  height: 500rpx;
  top: -100rpx;
  right: -100rpx;
  background: radial-gradient(circle, rgba(255, 255, 255, 0.1) 0%, transparent 70%);
}

.bg-circle--2 {
  width: 400rpx;
  height: 400rpx;
  bottom: -150rpx;
  left: -80rpx;
  background: radial-gradient(circle, rgba(255, 224, 178, 0.15) 0%, transparent 60%);
}

.bg-circle--3 {
  width: 120rpx;
  height: 120rpx;
  top: 20%;
  left: 20%;
  background: rgba(255,255,255,0.05);
  filter: blur(20rpx);
}

/* 噪点纹理 - 增加高级感 */
.bg-pattern {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  opacity: 0.15;
  background-image: radial-gradient(#fff 1px, transparent 1px);
  background-size: 30rpx 30rpx;
}

.header__content {
  position: relative;
  z-index: 2;
}

/* 顶部问候 */
.header__top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 40rpx;
}

.greeting-text {
  font-size: 60rpx;
  font-weight: 800;
  color: #FFD700;
  text-shadow: 0 4rpx 12rpx rgba(0,0,0,0.3);
  letter-spacing: 2rpx;
  line-height: 1.2;
}

.year-badge {
  background: rgba(255, 255, 255, 0.2);
  backdrop-filter: blur(16rpx);
  padding: 10rpx 28rpx;
  border-radius: 100rpx;
  font-size: 28rpx;
  font-weight: 700;
  color: #FFECB3; /* 金色字体 */
  border: 2rpx solid rgba(255, 236, 179, 0.3);
  box-shadow: 0 4rpx 12rpx rgba(0,0,0,0.1);
}

/* 倒计时 */
.countdown {
  color: #FFF8E7;
  margin-bottom: 32rpx;
}

.countdown__label {
  font-size: 28rpx;
  opacity: 1;
  margin-bottom: 8rpx;
  display: block;
  letter-spacing: 1rpx;
}

.countdown__row {
  display: flex;
  align-items: baseline;
}

.countdown__days {
  font-family: 'DIN Alternate', 'Helvetica Neue', sans-serif; /* 数字字体优化 */
  font-size: 100rpx;
  font-weight: 900;
  line-height: 0.9;
  color: #FFD700;
  margin-right: 12rpx;
  text-shadow: 0 4rpx 20rpx rgba(0,0,0,0.3);
}

.countdown__unit {
  font-size: 32rpx;
  opacity: 0.9;
  font-weight: 500;
}

/* 提示气泡 */
.header__tip {
  display: inline-flex;
  align-items: center;
  background: rgba(0, 0, 0, 0.15);
  padding: 12rpx 28rpx;
  border-radius: 100rpx;
  border: 1rpx solid rgba(255, 255, 255, 0.1);
}

.tip-icon { font-size: 28rpx; margin-right: 10rpx; }
.tip-text { font-size: 24rpx; color: #FFF8E7; }

/* 福字装饰容器 */
.fu-decoration {
  position: absolute;
  top: 50%;
  right: 16rpx;
  transform: translateY(-50%);
  width: 200rpx;
  height: 240rpx;
  pointer-events: none;
}

/* 背景装饰圆 */
.fu-bg-circle-1,
.fu-bg-circle-2,
.fu-bg-circle-3 {
  position: absolute;
  border-radius: 50%;
  border: 2rpx solid;
  pointer-events: none;
}

.fu-bg-circle-1 {
  width: 180rpx;
  height: 180rpx;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  border-color: rgba(255, 215, 0, 0.4);
  animation: pulse 3s ease-in-out infinite;
}

.fu-bg-circle-2 {
  width: 150rpx;
  height: 150rpx;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  border-color: rgba(211, 47, 47, 0.35);
  animation: pulse 3s ease-in-out infinite 0.5s;
}

.fu-bg-circle-3 {
  width: 120rpx;
  height: 120rpx;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  border-color: rgba(255, 215, 0, 0.3);
  animation: pulse 3s ease-in-out infinite 1s;
}

@keyframes pulse {
  0%, 100% {
    opacity: 0.8;
    transform: translate(-50%, -50%) scale(1);
  }
  50% {
    opacity: 1;
    transform: translate(-50%, -50%) scale(1.05);
  }
}

/* 祥云装饰 */
.cloud {
  position: absolute;
  font-size: 28rpx;
  opacity: 0.6;
  animation: float 4s ease-in-out infinite;
}

.cloud-1 {
  top: 20rpx;
  left: 10rpx;
  animation-delay: 0s;
}

.cloud-2 {
  bottom: 30rpx;
  right: 5rpx;
  animation-delay: 2s;
}

@keyframes float {
  0%, 100% {
    transform: translateY(0) translateX(0);
  }
  50% {
    transform: translateY(-10rpx) translateX(5rpx);
  }
}

/* 鞭炮装饰 */
.firework {
  position: absolute;
  font-size: 32rpx;
  opacity: 0.7;
  animation: shake 0.5s ease-in-out infinite;
}

.firework-1 {
  top: 10rpx;
  right: 20rpx;
}

.firework-2 {
  bottom: 20rpx;
  left: 15rpx;
  animation-delay: 0.2s;
}

@keyframes shake {
  0%, 100% {
    transform: rotate(-5deg);
  }
  50% {
    transform: rotate(5deg);
  }
}

/* 主福字方块 */
.fu-square {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%) rotate(45deg);
  width: 110rpx;
  height: 110rpx;
  background: linear-gradient(135deg,
    rgba(211, 47, 47, 0.2) 0%,
    rgba(255, 215, 0, 0.15) 50%,
    rgba(211, 47, 47, 0.2) 100%);
}

.fu-square-border {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  border: 4rpx solid rgba(211, 47, 47, 0.5);
  border-radius: 8rpx;
}

.fu-square-border::before {
  content: '';
  position: absolute;
  top: 6rpx;
  left: 6rpx;
  right: 6rpx;
  bottom: 6rpx;
  border: 3rpx solid rgba(255, 215, 0, 0.4);
  border-radius: 4rpx;
}

/* 福字 */
.fu-character {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%) rotate(-45deg);
  font-family: "KaiTi", "STKaiti", "楷体", serif;
  font-size: 60rpx;
  font-weight: 900;
  color: #FFD700;
  text-shadow:
    0 0 20rpx rgba(255, 215, 0, 0.6),
    2rpx 2rpx 6rpx rgba(211, 47, 47, 0.4),
    0 0 40rpx rgba(255, 215, 0, 0.4);
  z-index: 10;
}

/* 春节对联 */
.chun-char-top,
.chun-char-bottom {
  position: absolute;
  left: 50%;
  transform: translateX(-50%);
  font-family: "KaiTi", "STKaiti", "楷体", serif;
  font-size: 28rpx;
  font-weight: 700;
  color: rgba(211, 47, 47, 0.5);
  text-shadow: 1rpx 1rpx 3rpx rgba(255, 215, 0, 0.3);
}

.chun-char-top {
  top: 25rpx;
}

.chun-char-bottom {
  bottom: 25rpx;
}

/* 灯笼装饰 */
.lantern-decoration {
  position: absolute;
  font-size: 40rpx;
  opacity: 0.7;
  animation: swing 3s ease-in-out infinite;
}

.lantern-1 {
  top: 0;
  right: -5rpx;
}

.lantern-2 {
  bottom: 0;
  left: -5rpx;
  animation-delay: 1.5s;
}

@keyframes swing {
  0%, 100% {
    transform: rotate(-15deg);
  }
  50% {
    transform: rotate(15deg);
  }
}

/* 花纹装饰 */
.pattern {
  position: absolute;
  width: 24rpx;
  height: 24rpx;
  border: 2rpx solid rgba(255, 215, 0, 0.35);
  border-radius: 50%;
}

.pattern-1 {
  top: 40rpx;
  left: 20rpx;
}

.pattern-2 {
  top: 40rpx;
  right: 20rpx;
}

.pattern-3 {
  bottom: 40rpx;
  left: 20rpx;
}

.pattern-4 {
  bottom: 40rpx;
  right: 20rpx;
}


/* 功能入口 - 卡片上浮效果 */
.features {
  margin-top: -60rpx;
  padding: 0 32rpx;
  position: relative;
  z-index: 10;
}

.features__title {
  display: none; /* 隐藏标题，直接展示卡片更简洁 */
}

.features__grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 24rpx;
}

.feature-card {
  position: relative;
  background: linear-gradient(135deg, #FFF8F0 0%, #FFF 50%, #FFF8E7 100%);
  border-radius: 28rpx;
  padding: 36rpx 32rpx;
  box-shadow: 0 8rpx 24rpx rgba(211, 47, 47, 0.12);
  transition: all 0.3s ease;
  overflow: hidden;
  border: 2rpx solid rgba(255, 215, 0, 0.3);
}

/* 卡片四角装饰 */
.feature-card::before {
  content: '';
  position: absolute;
  top: 8rpx;
  left: 8rpx;
  width: 16rpx;
  height: 16rpx;
  border-top: 2rpx solid rgba(211, 47, 47, 0.2);
  border-left: 2rpx solid rgba(211, 47, 47, 0.2);
  border-radius: 4rpx 0 0 0;
}

.feature-card::after {
  content: '';
  position: absolute;
  bottom: 8rpx;
  right: 8rpx;
  width: 16rpx;
  height: 16rpx;
  border-bottom: 2rpx solid rgba(211, 47, 47, 0.2);
  border-right: 2rpx solid rgba(211, 47, 47, 0.2);
  border-radius: 0 0 4rpx 0 0;
}

.feature-card:active {
  transform: scale(0.98);
}

/* 卡片装饰字 */
.card-decoration {
  position: absolute;
  bottom: -25rpx;
  right: -15rpx;
  width: 70rpx;
  height: 70rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: "KaiTi", "STKaiti", "楷体", serif;
  font-size: 40rpx;
  font-weight: 900;
  color: rgba(211, 47, 47, 0.4);
  background: rgba(255, 245, 245, 0.6);
  border: 3rpx solid rgba(211, 47, 47, 0.3);
  border-radius: 50%;
  pointer-events: none;
  z-index: 1;
  transform: rotate(-15deg);
  box-shadow: 0 2rpx 8rpx rgba(211, 47, 47, 0.1);
}

/* 图标包裹容器 */
.icon-wrapper {
  position: relative;
  width: 100rpx;
  height: 100rpx;
  margin-bottom: 20rpx;
}

/* 图标背景 */
.icon-bg {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: linear-gradient(135deg, rgba(255, 215, 0, 0.2), rgba(211, 47, 47, 0.15));
  border-radius: 50%;
}

/* 图标 */
.feature-card__icon {
  position: relative;
  z-index: 2;
  font-size: 64rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  height: 100%;
  filter: drop-shadow(0 4rpx 6rpx rgba(211, 47, 47, 0.2));
}

.feature-card__title {
  font-size: 32rpx;
  font-weight: 700;
  color: #D32F2F;
  margin-bottom: 8rpx;
  position: relative;
  z-index: 2;
}

.feature-card__desc {
  font-size: 24rpx;
  color: var(--text-secondary);
  line-height: 1.3;
  position: relative;
  z-index: 2;
}

.feature-card__badge {
  position: absolute;
  top: -5rpx;
  right: -5rpx;
  background: rgba(211, 47, 47, 0.9);
  color: rgba(255, 215, 0, 0.8);
  font-size: 20rpx;
  padding: 8rpx 12rpx;
  border-radius: 50rpx;
  font-weight: 600;
  box-shadow: 0 2rpx 8rpx rgba(211, 47, 47, 0.25);
  border: 2rpx solid rgba(211, 47, 47, 0.3);
  z-index: 3;
  min-width: 60rpx;
  text-align: center;
}


/* 广告位 */
.ad-banner {
  padding: 0 32rpx 32rpx;
}

.ad-placeholder {
  background: linear-gradient(135deg, #FFF8E7, #FFE4B5);
  border-radius: 24rpx;
  border: 2rpx dashed #FFD700;
  padding: 48rpx 32rpx;
  text-align: center;
  box-shadow: 0 4rpx 16rpx rgba(211, 47, 47, 0.1);
  position: relative;
  overflow: hidden;
}

.ad-placeholder::before {
  content: 'AD';
  position: absolute;
  top: 16rpx;
  right: 16rpx;
  font-size: 20rpx;
  font-weight: 700;
  color: rgba(211, 47, 47, 0.2);
  padding: 4rpx 12rpx;
  border-radius: 8rpx;
  border: 1rpx solid rgba(211, 47, 47, 0.1);
}

.ad-icon {
  display: block;
  font-size: 64rpx;
  margin-bottom: 16rpx;
}

.ad-text {
  display: block;
  font-size: 32rpx;
  font-weight: 700;
  color: #D32F2F;
  margin-bottom: 8rpx;
}

.ad-desc {
  display: block;
  font-size: 24rpx;
  color: var(--text-secondary);
}


/* 春节小知识 - 卡片化 */
.tips {
  padding: 48rpx 32rpx 64rpx;
  position: relative;
}

.tips__title {
  font-size: 36rpx;
  font-weight: 800;
  color: #D32F2F;
  margin-bottom: 24rpx;
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.tips__title::before {
  content: '💡';
  font-size: 32rpx;
}

.tips__title::after {
  content: '';
  flex: 1;
  height: 2rpx;
  background: linear-gradient(90deg, rgba(211, 47, 47, 0.3), transparent);
}

.tips__content {
  background: #fff;
  border-radius: 24rpx;
  padding: 40rpx;
  box-shadow: var(--shadow-sm);
  border: 1rpx solid rgba(0,0,0,0.03);
}

.tip-item {
  display: flex;
  margin-bottom: 32rpx;
}
.tip-item:last-child { margin-bottom: 0; }

.tip-item__icon {
  font-size: 36rpx;
  margin-right: 20rpx;
  background: #F5F7FA;
  width: 64rpx;
  height: 64rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.tip-item__text {
  font-size: 26rpx;
  color: var(--text-secondary);
  line-height: 1.6;
  padding-top: 10rpx;
}

/* 刷新按钮 */
.tips__refresh {
  position: absolute;
  bottom: -15rpx;
  right: 40rpx;
  display: flex;
  align-items: center;
  gap: 8rpx;
  background: linear-gradient(135deg, #D32F2F, #B71C1C);
  color: #FFD700;
  padding: 12rpx 24rpx;
  border-radius: 50rpx;
  box-shadow: 0 4rpx 12rpx rgba(211, 47, 47, 0.3);
  border: 2rpx solid rgba(255, 215, 0, 0.3);
  z-index: 2;
  transition: all 0.3s;
}

.tips__refresh:active {
  transform: scale(0.95);
  box-shadow: 0 2rpx 8rpx rgba(211, 47, 47, 0.3);
}

.refresh-icon {
  font-size: 24rpx;
}

.refresh-text {
  font-size: 24rpx;
  font-weight: 600;
}
